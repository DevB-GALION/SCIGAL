package com.dim.ws;

// This class was a JSR-356 (jakarta.websocket) endpoint in the original project.
// In this build we use an embedded Vert.x WebSocket server instead. Keep a
// placeholder class so builds that reference the type (if any) still compile.
public class RawDataWebSocket {
    // Placeholder — actual WS handling done by VertxWebSocketServer
}
